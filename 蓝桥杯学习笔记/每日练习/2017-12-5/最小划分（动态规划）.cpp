#include<stdio.h>
#include<math.h>
#define max 100
int min(int a[],int length)
{
	int i;int a1[max];int min;
	min()
	for(i=0;i<length;i++)
	{
		
	}
}
int main()
{
	int a[max]={1,5,6,11};//可以对数组进行排序
	int length=4,num;//length获取数组长度 
	num=min(a,length);
	printf("%d",num); 
	return 0;
 } 
